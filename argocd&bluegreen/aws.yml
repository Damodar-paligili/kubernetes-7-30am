apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-myapp
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
      replica: red
  template:
    metadata:
      labels:
        app: myapp
        replica: red
    spec:
      containers:
        - name: myapp
          image: 815512685331.dkr.ecr.us-east-1.amazonaws.com/aws:latest
          ports:
            - name: http
              containerPort: 80
